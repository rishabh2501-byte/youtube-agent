# This directory stores OAuth credentials
# Do not commit actual credential files to version control
# 
# Required files:
# - client_secrets.json (from Google Cloud Console)
# - youtube_token.json (generated after first OAuth flow)
